import React from "react";
import {expect} from "chai";

describe("a test", () =>
{
    it("test array", () =>
    {
        let array = [];
        
        expect(array.length).to.equal(0);
    });
});